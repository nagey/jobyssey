<h1>All Jobs</h1>
<p>Here is a list of all of the open positions on Jobyssey at the moment. </p>

<% for employer in @employers %>
  <h2><%= employer.name %></h2>
  <table>
    <% for job in employer.job_postings %>
    <tr><td><%= link_to job.title, :controller => "job", :action => "view", :id => job %> at <%= link_to job.employer.name, :controller => "company_profiles", :action => "view", :id => job.employer_id %></td></tr>
    <% end %>
  </table>
<% end %>